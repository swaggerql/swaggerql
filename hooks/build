#!/bin/bash

echo "BASE_CONTAINER=$BASE_CONTAINER"
echo "DRIVER_MODULE=$DRIVER_MODULE"
docker build --build-arg BASE_CONTAINER=$BASE_CONTAINER --build-arg DRIVER_MODULE=$DRIVER_MODULE -f $DOCKERFILE_PATH -t $IMAGE_NAME .
